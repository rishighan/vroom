 <% @posts.each do |post| %>
    <article>
    <h2> <%= post.title %> </h2>
    <p>
        <%= markdown(post.content) %>
        <small><%= post.citations %> </small>
    </p>

    </article>
    <%= link_to "Edit Post", edit_post_path(post) %>
 <% end %>

